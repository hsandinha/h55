"use client";

// Cena 3D removida a pedido (substituída por fundo de vídeo/foto no HeroSection).
// Mantido como stub para não quebrar imports antigos e remover a dependência "three".
export function Hero3D() {
  return null;
}
